export default function ParticleBurst(){return <div className="pointer-events-none absolute inset-0 overflow-hidden">{Array.from({length:42}).map((_,i)=><span key={i} className="absolute animate-ping" style={{left:'50%',top:'50%',transform:`translate(${Math.cos(i)*220}px,${Math.sin(i)*220}px)`,color:['#FF3D8B','#F5D547','#FBF5E4'][i%3],animationDelay:`${i*20}ms`}}>✦</span>)}</div>}
