import type{Rarity}from'../../types';import{rarityMeta}from'../../lib/rarityEngine';export default function RarityOverlay({rarity}:{rarity:Rarity}){const cls=rarity==='RARE'?'shimmer':rarity==='LEGENDARY'?'legendary-particles':rarity==='MYTHIC'?'aurora':rarity==='EPIC'?'holo opacity-60':'';return <><div className={`pointer-events-none absolute inset-0 rounded-[2rem] ${cls}`}/><div className="absolute left-4 top-4 z-20 rounded-full border px-3 py-1 text-xs font-black" style={{borderColor:rarityMeta[rarity].color,color:rarityMeta[rarity].color,boxShadow:`0 0 25px ${rarityMeta[rarity].color}55`}}>{rarity}</div></>}
