import{fonts,patterns}from'../../data/palettes';export default function TextControls({font,setFont,textColor,setTextColor,glow,setGlow,pattern,setPattern}:{font:string,setFont:(v:string)=>void,textColor:string,setTextColor:(v:string)=>void,glow:boolean,setGlow:(v:boolean)=>void,pattern:string,setPattern:(v:string)=>void}){return <div className="grid gap-3"><select value={font} onChange={e=>setFont(e.target.value)} className="rounded-xl bg-ink/60 p-3">{fonts.map(f=><option key={f}>{f}</option>)}</select><select value={pattern} onChange={e=>setPattern(e.target.value)} className="rounded-xl bg-ink/60 p-3">{patterns.map(f=><option key={f}>{f}</option>)}</select><label className="flex items-center gap-2 text-sm"><input type="color" value={textColor} onChange={e=>setTextColor(e.target.value)}/> Text color</label><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={glow} onChange={e=>setGlow(e.target.checked)}/> Glow text/frame</label></div>}
