import{stickers}from'../../data/stickers';import type{PlacedSticker}from'../../types';export default function StickerPanel({onAdd}:{onAdd:(s:PlacedSticker)=>void}){return <><p className="mb-3 text-xs text-cream/60">Click to add, then drag the sticker directly on the live card.</p><div className="grid grid-cols-6 gap-2">{stickers.map(s=><button key={s.id} title={s.label} onClick={()=>onAdd({...s,x:15+Math.random()*70,y:15+Math.random()*70,r:Math.random()*40-20,s:1+Math.random()*.5})} className="rounded-xl bg-ink/50 p-2 text-2xl hover:bg-pink-hot/30">{s.emoji}</button>)}</div></>}
