import{getArchetype}from'../../data/archetypes';export default function ColorPicker({color,arch,onChange}:{color:string,arch:string,onChange:(v:string)=>void}){const a=getArchetype(arch);return <div><label className="text-xs text-cream/60">Frame color</label><div className="mt-2 flex items-center gap-2"><input type="color" value={color} onChange={e=>onChange(e.target.value)} className="h-10 w-16 rounded bg-transparent"/>{a.palette.map(c=><button key={c} onClick={()=>onChange(c)} className="h-9 w-9 rounded-full border border-cream/30" style={{background:c}}/> )}</div></div>}
