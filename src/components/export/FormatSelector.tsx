import type{ExportFormat}from'../../lib/exportCard';export const formats:ExportFormat[]=['png','twitter','square','story','whatsapp','pdf','gif'];export default function FormatSelector({value,onChange}:{value:ExportFormat,onChange:(v:ExportFormat)=>void}){return <select value={value} onChange={e=>onChange(e.target.value as ExportFormat)} className="w-full rounded-xl bg-ink/60 p-3">{formats.map(f=><option key={f} value={f}>{f.toUpperCase()}</option>)}</select>}
