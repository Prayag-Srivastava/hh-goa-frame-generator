import toast from'react-hot-toast';import{Share2,MessageCircle,Link as LinkIcon}from'lucide-react';export default function ShareButtons({id}:{id:string}){const url=location.origin+'/card/'+id;return <div className="flex flex-wrap gap-2"><a className="btn bg-sky-500 text-white" target="_blank" href={`https://twitter.com/intent/tweet?text=${encodeURIComponent('I just got framed in Goa 🌴 #FrameInGoa #HackerHouseGoa')}&url=${encodeURIComponent(url)}`}><Share2 size={16}/>Tweet</a><a className="btn bg-green-500 text-white" target="_blank" href={`https://wa.me/?text=${encodeURIComponent('My GOA//FRAME: '+url)}`}><MessageCircle size={16}/>WhatsApp</a><button className="btn bg-cream text-ink" onClick={()=>navigator.clipboard.writeText(url).then(()=>toast.success('Link copied'))}><LinkIcon size={16}/>Copy Link</button></div>}
