import{Link}from'react-router-dom';import type{CardData}from'../../types';import CardCanvas from'../cards/CardCanvas';export default function CardGrid({cards}:{cards:CardData[]}){return <div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{cards.map(c=><Link to={`/card/${c.id}`} key={c.id} className="transition hover:scale-105"><div className="origin-top-left scale-[.62] sm:scale-[.55] h-[360px]"><CardCanvas card={c}/></div></Link>)}</div>}
