import type{CardData,Rarity}from'../../types';import{rarityMeta}from'../../lib/rarityEngine';const rarities: Rarity[]=['COMMON','RARE','EPIC','LEGENDARY','MYTHIC'];export default function RarityChart({cards}:{cards:CardData[]}){return <div className="grid gap-2">{rarities.map(r=>{const n=cards.filter(c=>c.rarity===r).length;const pct=cards.length?n/cards.length*100:0;return <div key={r}><div className="flex justify-between text-xs"><b style={{color:rarityMeta[r].color}}>{r}</b><span>{n} · {pct.toFixed(1)}%</span></div><div className="h-3 rounded bg-ink/60"><div className="h-full rounded" style={{width:pct+'%',background:rarityMeta[r].color}}/></div></div>})}</div>}
