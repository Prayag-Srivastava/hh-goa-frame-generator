import{useEffect,useState}from'react';import toast from'react-hot-toast';import{installEasterEggs}from'../lib/easterEggs';export function useEasterEggs(){const[mode,setMode]=useState('');useEffect(()=>installEasterEggs(s=>{setMode(s);toast.success(s==='golden'?'Secret Golden Hour frame unlocked!':`Easter egg: ${s.toUpperCase()}`);document.body.classList.toggle('palm-cursor',s==='konami');if(s==='susegad'){document.body.style.transition='filter 1s';document.body.style.filter='saturate(.75) blur(.2px)';setTimeout(()=>document.body.style.filter='',5000)}}),[]);return mode}
