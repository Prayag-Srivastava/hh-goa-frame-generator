import{useMotionValue,useSpring,useTransform}from'framer-motion';import{MouseEvent}from'react';export function useMouseTilt(max=15){const x=useMotionValue(0),y=useMotionValue(0);const sx=useSpring(x,{stiffness:180,damping:18}),sy=useSpring(y,{stiffness:180,damping:18});const rotateY=useTransform(sx,[-.5,.5],[-max,max]);const rotateX=useTransform(sy,[-.5,.5],[max,-max]);function onMouseMove(e:MouseEvent<HTMLElement>){const r=e.currentTarget.getBoundingClientRect();x.set((e.clientX-r.left)/r.width-.5);y.set((e.clientY-r.top)/r.height-.5);e.currentTarget.style.setProperty('--mx',`${((e.clientX-r.left)/r.width)*100}%`);e.currentTarget.style.setProperty('--my',`${((e.clientY-r.top)/r.height)*100}%`)}function onMouseLeave(){x.set(0);y.set(0)}return{style:{rotateX,rotateY,transformStyle:'preserve-3d' as const},onMouseMove,onMouseLeave}}
