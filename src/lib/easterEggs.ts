export function installEasterEggs(onSecret:(s:string)=>void){let typed='';let logo=0,sun=0;const konami=['ArrowUp','ArrowUp','ArrowDown','ArrowDown','ArrowLeft','ArrowRight','ArrowLeft','ArrowRight','b','a'];let seq:string[]=[];const key=(e:KeyboardEvent)=>{typed=(typed+e.key.toLowerCase()).slice(-20);if(typed.includes('susegad'))onSecret('susegad');if(typed.includes('goa'))onSecret('goa');seq=[...seq,e.key].slice(-10);if(seq.join(',')===konami.join(','))onSecret('konami')};document.addEventListener('keydown',key);(window as any).goaLogoClick=()=>{if(++logo>=5)onSecret('credits')};(window as any).goaSunClick=()=>{if(++sun>=10)onSecret('golden')};return()=>document.removeEventListener('keydown',key)}
