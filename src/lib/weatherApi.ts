export async function getGoaWeather(){try{const key=import.meta.env.VITE_OPENWEATHER_KEY;if(!key)throw new Error('mock');const r=await fetch(`https://api.openweathermap.org/data/2.5/weather?q=Panaji,IN&units=metric&appid=${key}`);const j=await r.json();return `${Math.round(j.main.temp)}°C · ${j.weather[0].main} · Panjim`}catch{return '31°C · Sunny · Panjim'}}export const goaTime=()=>new Intl.DateTimeFormat('en-IN',{timeZone:'Asia/Kolkata',hour:'2-digit',minute:'2-digit',second:'2-digit'}).format(new Date());
