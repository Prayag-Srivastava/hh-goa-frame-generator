import * as faceapi from'face-api.js';let loaded=false;export async function detectFace(dataUrl:string):Promise<{x:number;y:number;w:number;h:number}|null>{try{if(!loaded){await faceapi.nets.tinyFaceDetector.loadFromUri('https://cdn.jsdelivr.net/npm/@vladmandic/face-api/model/');loaded=true}const img=await new Promise<HTMLImageElement>(res=>{const i=new Image();i.onload=()=>res(i);i.src=dataUrl});const d=await faceapi.detectSingleFace(img,new faceapi.TinyFaceDetectorOptions({inputSize:320,scoreThreshold:.45}));if(!d)return null;const b=d.box;return{x:b.x/img.width,y:b.y/img.height,w:b.width/img.width,h:b.height/img.height}}catch{return null}}
