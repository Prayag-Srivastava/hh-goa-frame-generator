export type Rarity='COMMON'|'RARE'|'EPIC'|'LEGENDARY'|'MYTHIC';export type Mode='pfp'|'id';export type CardVariant=string;export interface Archetype{id:string;emoji:string;name:string;short:string;tagline:string;palette:string[];stats:{ship:number;vibe:number;feni:number}}export interface Sticker{id:string;emoji:string;label:string}export interface PlacedSticker extends Sticker{x:number;y:number;r:number;s:number}export interface CardData{id:string;createdAt:number;mode:Mode;name:string;role:string;crew:string;shipping:string;stack:string[];callsign:string;github:string;passCode:string;serial:string;archetypeId:string;rarity:Rarity;variant:string;photo?:string;filter:string;color:string;border:number;font:string;textColor:string;glow:boolean;pattern:string;stickers:PlacedSticker[];photoZoom:number;photoRotate:number;photoX:number;photoY:number;goaHq:string;weather:string;moon:string}
